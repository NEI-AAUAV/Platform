# rally Extension Routes (disabled)
# Proxy to main platform for consistent 404 handling

location ~ ^/(api|static)/rally(/.*)?$ {
    return 404;
}

location ~ ^/rally(/.*)?$ {
    proxy_pass http://web_nei:3000;
}
